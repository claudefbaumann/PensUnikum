{
  "name": "pensunikum-tests",
  "version": "1.0.0",
  "description": "Automated tests for PensUnikum core logic",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
